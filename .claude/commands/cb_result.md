---
description: An executed notebook has come back
argument-hint: [paste output, or a path]
---
Invoke the `reading-results` skill and follow it.

Check the assertions first, then diff the notebook that came back against the one issued,
then read the result. **If an assertion or check was deleted to make it run, say so
loudly** — that is invisible in the output and it means the run proves nothing.

Do not report a decomposition of an accounting identity as a finding.

Output or path: $ARGUMENTS
