---
description: Read new material from raw/ into the wiki
argument-hint: [optional: which file]
---
Invoke the `ingesting-raw` skill and follow it exactly.

Read `wiki/README.md` first, then look at what is in `raw/` that is not yet reflected in
the wiki. Company material and external material take different routes — the skill says
which.

`raw/` is immutable. Read it, never edit or delete it.

Finish with the summary the skill asks for: what was written where, what looked like a
duplicate concept, what is dateable, and anything you could not classify.

Files or focus (may be empty): $ARGUMENTS
